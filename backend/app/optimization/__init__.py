"""Engine 3 — Optimization. feasibility.py (Stage 6), strategies.py
(Stage 7, bounded archetypes + BR validation), ranking.py (Stage 9,
config-driven weights). Heuristic-first (D-7); OR-Tools only ever behind
generate_candidates() if a real failing case demands it. No DB, no LLM.
Built in build-plan Phase 4."""
