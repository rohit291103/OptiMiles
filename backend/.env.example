# Supabase Postgres (use the PgBouncer transaction-mode pooler URL, db-schema §6.5)
DATABASE_URL=postgresql+asyncpg://postgres:<password>@<project>.pooler.supabase.com:6543/postgres

# LLM (only read inside ai_reasoning/ — build rule 3). Unused until Phase 5.
LLM_PROVIDER=openai
LLM_API_KEY=
