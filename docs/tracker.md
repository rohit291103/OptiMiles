# OptiMiles — Project Tracker

A living snapshot of where the project actually stands right now — not a changelog. This file is **overwritten in place** after any session that does real work in `backend/`, `frontend/`, or `docs/`; it should never grow into an endless history. For *why* a decision was made, see `docs/decisions/` — that log is permanent and never rewritten. This file is disposable and just reflects current truth.

Maintained by the `tracker-sync` skill (`.claude/skills/tracker-sync/SKILL.md`). Read this file before starting new work in any chat; refresh it before ending one.

---

**Last updated:** 2026-07-15 (2nd sitting) — **The guided strategy-flow revamp is COMPLETE: all 10 slices shipped** (this sitting added slices 5–7, 9, 10: silent probe gate interrupting hopeless goals, the education step with shared-partner story, the opt-in categories-win split, the end-of-flow wallet-shortfall verdict + explicit extra-card ask revealing the labeled pair, and the homepage swap — the wizard is now THE goal-entry surface everywhere, the one-shot simulator is deleted, and LLM education framing rides on `GET /catalog/education/story` with number-echo validation and a 10s bound. 372 backend tests, mypy strict + ruff + tsc + eslint clean; three live frontend-reviewer passes + two backend-reviewer passes this sitting, all findings applied). _(1st sitting:)_ **Guided-flow slices 4 + 8 shipped** (the conversational-scroll wizard shell — goal → cards → total-spend → strategy, frontend-reviewer drove it end-to-end at both viewports with zero crit/important findings; the labeled cheapest/best-value acquisition pair via a versioned `acquisition_weights` profile in `ranking-weights-v2.yaml`, backend-reviewer's two asks applied). _(Prior sitting, 2026-07-14:)_ **Guided-flow backend slices 1–3 shipped** (total-spend budget on the simulate request, `GET /catalog/education` wallet reward story, `POST /simulations/probe` Stage 1–6 feasibility check — all TDD, backend-reviewer clean, all three live-verified via curl; slices ticked in the design doc). _(Prior sitting, 2026-07-13 2nd:)_ **Goal-page UI/UX polish shipped** (simulator form typography bump, bank-grouped card picker with check-dot tiles, gold active-goal hero + status chips gated on real `status`, save-in-progress spinner + honest "up to a minute" copy, auto-scroll to the finished strategy) and the **user's guided-strategy-flow revamp design grilled and LOCKED** (13 decisions, decision log `2026-07-13-guided-strategy-flow-design.md`; total-spend-over-horizon → card/ecosystem education → opt-in category split → visible calculations with LLM framing → end-of-flow feasibility check → extra-card strategies). frontend-reviewer drove the public simulator live at both viewports (clean) and caught one real regression (unconditional "Active goal" claim — fixed, gated on `goal.status`). tsc + eslint clean. Nothing committed. _(1st sitting:)_ **Runner-up cause attribution shipped: the strategy UI now says WHY a higher-rated card lost a category, verified by counterfactual** (`runner_up_reason` + `runner_up_plan_delta_miles` on every allocation row: transfer_cap / milestone / fewer_total / equal_total / route_shape; the Atlas-vs-TravelOne travel confusion now reads "its yearly transfer allowance is already fully used — moving this spend would end 52,000 miles lower overall"). Decision log `2026-07-13-runner-up-cause-attribution.md`. 314 backend tests, both reviewers ran + findings applied, verified live. _(2026-07-12, 3rd sitting:)_ **Infeasible goals now ship a best-effort plan + the adjustment menu** (Stages 7–9 un-gated; least-bad `misses_goal` route recommended, narration states the honest shortfall, menu persists in the JSONB, UI renders hero gap line + shared `AdjustmentMenu` on live and saved views). Decision log `2026-07-12-infeasible-best-effort-plan.md`. 305 backend tests, both reviewers ran, all findings applied, verified live. _(2nd sitting:)_ **Strategy output typography bumped one step everywhere and the per-card calculation is now a real table** (6 columns + Total row, `overflow-x-auto` internal scroll on mobile), guidance prose below the table; frontend-reviewer verified live, no crit/important findings. **Still open (see Next up): spend-model revamp (total-spend-over-horizon + reward-value-first education) and runner-up cause attribution.** _(Earlier same-day sitting:)_ **Strategy output now reads as ≤3 distinct routes with honest card-fee headlines and a full per-category math story.** User feedback on the saved-goal view drove five fixes: (1) route tabs capped at 3 genuinely different acquisition sets (`select_route_options`, Stage 9 → wired in `pipeline/run.py`; archetype variants of the same acquisition collapse to the best-ranked plan); (2) the "Card no longer listed" labels on alternative tabs were a real bug — `display_names()` only mapped the recommended strategy's ids — now covers every tier + runner-ups; (3) fees split into `card_fees_inr` (joining fees — what every headline/tab/narration now shows) vs `transfer_fees_inr` (Axis ₹235-type program fees — surfaced exactly once, as a transfer-step footnote; per-row valuation fee note removed at source); (4) `StrategyAllocationDetail` enriched into a reward-system story: monthly_miles worked example, currency name, transfer ratio, `category_label` ("Travel via SmartBuy portal" — how to actually get an accelerated rate), and a **runner-up comparison** (best other card available in the plan, per category — the deterministic answer to "why is TravelOne ignored?"); (5) SpendStep redesigned (rate chain → worked example → portal line → comparison; card-level note hoisting; duplicate narration summary dropped from the feasible hero). Both reviewers ran; backend clean (2 polish notes applied), frontend caught 3 real issues (missing JSX space, fee double-mention, a hardcoded-and-wrong "transfer caps" causal claim) — all fixed. **300 backend tests, mypy strict + ruff, tsc + eslint clean; live pipeline verified on the user's exact scenario.** Decision log `2026-07-12-route-selection-fee-split-and-earn-story.md`. Nothing committed. _(Prior 2026-07-11 sittings consolidated into Done bullets below.)_ The user tried the revamped UI live and found every acquisition option was Axis Magnus for Burgundy (₹30k), even though HDFC Diners Club Black Metal (₹10k) also clears the same goal — asked to fix the ranking engine directly. Root-caused to THREE compounding bugs across Stages 7/9 (all archetypes optimize for miles, never cost; `hill_climb` ignores its starting basin and always converges to the best-rate card; BR-02's "beat baseline by 5%" gate and the dominance prune both independently discard a cheap-but-real acquisition when the wallet's own status quo is already strong). Fixed with a new **`CHEAPEST_VIABLE` archetype** (forces spend onto the lowest-fee acquirable card that alone clears the goal, no hill-climb, exempt from BR-02) + a **scoped dominance-pruning exemption** (a goal-achieving acquiring candidate is never pruned for losing to a cheaper no-new-card option — "add one card" vs "use what I have" is a trade-off, not a strict downgrade; goal-missing acquisitions and acquiring-vs-acquiring dominance are unaffected). backend-reviewer found no critical/important issues; tightened the exemption to goal-achieving-only per its polish note. Frontend needed **zero changes** to render the new tier (route tabs already build generically from the response array) — frontend-reviewer verified live and caught one legibility gap (two archetypes can converge on the same card label, e.g. two "Add Magnus for Burgundy" tabs with different numbers) — fixed by appending the engine's `headline_differentiator` to disambiguate colliding tab labels. **286 backend tests (up from 283), mypy strict + ruff, tsc + eslint all clean.** Verified live end-to-end: the reported scenario now shows status quo (₹0, 59,565mi) → Diners Club (₹10,000, 51,282mi) → Magnus (₹30,235–30,470, 73,920–76,120mi). Decision log `2026-07-11-cheapest-viable-archetype-and-dominance-fix.md`. Nothing committed. **⚠️ Still open: rotate the OpenRouter key + Supabase DB password; `/simulations` measured 40–46s live this session (30s budget, getting worse not better) — narration LLM call is retrying on attempt 1 every request per backend logs, worth investigating directly.** _(Earlier 2026-07-11 sittings — the full strategy-UX narrative revamp, the strategy "story" backend slice, and the branded loaders + delete modal — are consolidated into the Done bullets below and their decision logs: `2026-07-11-strategy-ux-revamp-narrative-flow.md`, `2026-07-11-strategy-story-output.md`, `2026-07-11-loading-animation-and-delete-modal.md`.)_

## Snapshot

**Phase 1 — Backend Foundation** (entered 2026-07-03, per root `CLAUDE.md`). **The backend build plan (`docs/architecture/backend-build-plan-v1.md`) is complete through Phase 6, and Phase 7 (Integration) is underway:** all five engines built and fixture-tested + the pipeline orchestrator + API v1, and now the **public Goal Simulator is live against the real engine** (`POST /simulations`, anonymous, no DB, no LLM key — structured intent skips Stage 1, narration is template-fallback). `pipeline/run.py` runs goal text → full Recommendation Package with **no DB and no LLM key required**; the determinism invariant is a green standing test. **Auth + persistence are LIVE end-to-end** (Supabase Auth + FastAPI JWT + live Supabase Postgres via the transaction pooler). A user logs in with Google → `api/auth.py` verifies the token (fails closed, `alg:none`/confusion blocked, `exp`/`aud`/`sub` required) → the session-aware nav shows them → the simulator's "Save this goal" button hits `POST /goals/recommendation/save` (`persist=True`) → the full lineage chain writes under their real `auth.users` id, and the endpoint reports `persisted` so the UI never claims a save that didn't land. Proven against the live DB with a real Google account. The signed-in surface is now a real dashboard: an app shell (sidebar/top bar/auth gate) whose home lists saved goals with a focus-goal hero, each opening into its full stored strategy (`GET /goals/{id}`, pure persisted-artifact reconstruction), with in-app goal creation at `/goals/new`. What's NOT yet built (follow-ups, not blockers): free-text goal entry in the UI; an automated real-DB round-trip test; score-breakdown/verdict persistence for saved-goal detail parity. Open risks: (a) ~60% of seed rows still carry `needs_verification: true`; (b) no LLM API key in `.env` yet (narration is template-fallback; D-5 streaming deferred until a live key is measured); (c) **Supabase DB password transited chat during debugging — rotate when convenient** (user has deferred); (d) snapshot loads once and caches — a transient cold-pooler refusal at first request serves 500s until restart (retry is a noted follow-up). Frontend is a product-first, edge-to-edge marketing site (dark/gold, Framer Motion v12) with a now-live Goal Simulator (full explainable strategy detail + Save). Repo: `github.com/rohit291103/OptiMiles`.

---

## Backend

### Done
- **Skeleton (build-plan Phase 0):** `backend/` laid out per plan §4 (`app/{domain,knowledge,valuation,simulation,optimization,ai_reasoning,pipeline,repositories,api}`, `seeds/`, `tests/`), each engine package carrying its ownership rules as a docstring. uv-managed, Python 3.13; ruff + **mypy strict** + pytest green; CI at `.github/workflows/backend-ci.yml` (path-filtered to `backend/`). FastAPI boots; `GET /health` 200 (`catalog_snapshot_version` stays null until the pipeline phase wires a DB-backed snapshot).
- **`app/domain/` shared kernel:** frozen Pydantic models for every blueprint pipeline object (intent → goal → requirement → PlanningContext → opportunities → feasibility/PortfolioAssessment → strategies → simulation → ranking → narration → FinalRecommendation) + v1.1 catalog types (`RewardCurrency`, `CurrencyTransferLink`) + canonical enums. All-frozen enforced by test.
- **Schema v1.1 live on Supabase** (project `lrmmoianbzjghlpxcisl`, Tokyo/`ap-northeast-1`): Alembic migrations `0001`–`0003` applied & verified — 14 tables, currency-level transfer junction, lineage columns, and **RLS on ALL public tables** (8 user-scoped from `0001`; the 7 catalog tables got RLS + a public read-only SELECT policy in `0003_catalog_rls_read_only`, service-role bypasses for the app's own reads/writes). **Connect via the transaction-mode POOLER** (`postgres.<ref>@aws-0-ap-northeast-1.pooler.supabase.com:6543`), NOT the direct host (IPv6-only, times out); `statement_cache_size=0` is set on both the app engine (`deps.py`) and the migration engine (`env.py`) — REQUIRED, else PgBouncer transaction mode raises `DuplicatePreparedStatementError`. Durable constraint: migration SQL routes through a `_execute()` splitter (asyncpg = one command per prepared statement) — never put `;` inside SQL string literals in migrations. Decision log: `2026-07-06-db-pooler-fix-and-catalog-rls.md`.
- **Boundary guards as tests** (`tests/unit/test_module_boundaries.py`): AST checks — `domain/` imports nothing from `app`, engines don't cross-import (except Optimization/Simulation → Valuation pure math), no LLM client outside `ai_reasoning/`.
- **Knowledge Engine (build-plan Phase 1, test-first):** `app/knowledge/` — `seed_catalog.py` (YAML → CatalogSnapshot, deterministic UUID5 ids), `versioning.py` (Decimal-normalized content-hash version: same content ⇒ same version from YAML or DB), `validation.py` (`validate_catalog()`: orphan FKs, default-category + duplicate-slug checks, ratio sanity 1:4…5:1, cashback-has-no-links negative case, KrisFlyer reach, 3-MVP-routes × business coverage — reports ALL issues at once), `goal_resolution.py` (Stage 2: city→region table; ambiguous/unknown input → ClarificationRequest, un-charted route → explicit `UnsupportedRoute`), `requirements.py` (Stage 3: locked-chart lookup, buffer ceils up, `ChartRowMissing` fails loudly), `store.py` (sole DB reader of catalog tables, ORDER BY id). Writes live in `repositories/catalog_admin.py` (idempotent upsert + deactivate-stale, never delete).
- **Seed catalog, 9 cards + hotel programs** (`seeds/catalog/*.yaml`, expanded + web-verified 2026-07-04 — decision log `2026-07-04-catalog-expansion-and-web-verification.md`): 7 currencies, 4 partners (KrisFlyer, Maharaja Club, **Marriott Bonvoy**, **Accor ALL** — hotel type, transfer-capability only), 14 transfer links (Infinia 1:1 KrisFlyer / 2:1 Bonvoy+Accor+Maharaja; Regalia 2:1 / 100:33 Bonvoy; HSBC 1:1 everywhere; Atlas 1:2 w/ 30k cap + ₹235 fee; **Magnus Burgundy 5:4 w/ 2L cap**; Amex 2:1 KrisFlyer / 1:1 Bonvoy), Atlas full milestone ladder (3L/7.5L/15L), **award chart corrected to post-Nov-2025 Zone 6 saver values** (SEA business 45,000 — was wrongly 35,000). Corrections caught: Maharaja was mis-seeded 1:1 (is 2:1); HDFC KrisFlyer processing is 7–10 days; Axis removed Marriott/Accor Apr-2026; Atlas discontinued for new applicants. Every row carries `source` + `verified_on`. **⚠️ Live Supabase still has the OLD catalog — reload via `seeds/load_to_db.py` after the user's line-by-line seed review.**

- **Valuation Engine (build-plan Phase 2, test-first):** `app/valuation/transfer_math.py` — the shared calculation vocabulary (formulas + units + rounding directions in the docstring): whole-block transfer conversion (`floor(points/ratio_from)×ratio_to` + `whole_block_transfer` exposing the points-sent side for ledgers), min-threshold + annual-cap gating, `miles_per_100` (4dp ROUND_DOWN), cap-aware `blended_earn_rate`. `app/valuation/opportunities.py` — Stage 5: one opportunity per (eligible card × profile category), eligibility card→currency→link, default-rule fallback, valuation notes for caps/fees/default-rate, deterministic order; SBI yields zero opportunities by construction. Documented deviations (decision log `2026-07-04-phase2-...`): flat fees not folded into rates (no groundable miles→INR value); annual transfer caps applied once (exact for ≤12-month horizons). Reviewer recomputed all goldens (all matched) and caught a `normalize()`→scientific-notation serialization bug (fixed, str()-regression-tested) + unpinned cards (DCB/Regalia/HSBC now golden-asserted, incl. a D-1 currency-sharing canary).

- **Simulation Engine (build-plan Phase 3, test-first):** `app/simulation/projector.py` — `simulate(strategy, context)`, the Stage 8 pure month-by-month ledger and the **cap-truth layer correcting Stage 5's static blend (reviewer-confirmed: reads raw category rules, no double-counting)**. Semantics (full contract in module docstring + decision log `2026-07-04-phase3-simulation-engine-semantics.md`): 0-based months, full horizon always simulated (goal recorded at first achieving month, ledger continues for Ranking); within-month order spend → cap-aware earn (one floor per card-month, monthly caps reset) → milestones (≥ thresholds; quarterly re-fires per plan quarter, annual/one-time fire once, welcome for acquired cards only) → whole-block transfers via `transfer_math.whole_block_transfer` (remainder stays on card; annual link cap tracked cumulatively; arrival = planned + ceil(processing_days_max/30), miles landing ≥ horizon never count) → goal progress. Fail-loud guards: partial strategies, unallocated categories, quarterly/annual rule caps (unsupported in v1), and `validate_catalog()` now rejects milestone validity windows until BR-05/06 are implementable.

- **Optimization Engine (build-plan Phase 4, test-first):** `app/optimization/` (Stages 6/7/9), decision log `2026-07-05-phase4-optimization-engine-semantics.md`. `allocation.py` — shared internal math: `bound_miles` (optimistic, currency-cap-aware, floors once), `greedy_assignment`/`hill_climb` (best single-category reroute until no gain — IS both cap-reroute and milestone-chasing), `claimed_estimate` (mirrors the projector: same transfer cutoff, whole-block flooring, milestone periods — **reconciliation gap is exactly 0 on every archetype**). `feasibility.py` (Stage 6) — `assess_feasibility`: bound check (feasible ⇔ ≥ required, buffer excluded → `tight`), computed adjustment options (extend/add_card/raise_spend re-priced via Stage 5/downgrade_cabin, only when they actually work — a saturated cap yields no extend/raise), `PortfolioAssessment`. `strategies.py` (Stage 7) — `generate_candidates`: **5 archetypes** as hill-climb starting basins (status_quo_optimized, one_new_card, concentrated, simplest_viable, and **cheapest_viable** — 2026-07-11: the lowest-fee acquirable card that alone clears the goal, forced allocation with NO hill-climb since climbing converges to the best-rate card regardless of starting basin, exempt from BR-02's beat-baseline-by-5% gate), BR-01…07 validation at exit (discard, never patch), deterministic ids. `ranking.py` (Stage 9) — `rank`: reconcile → prune (dupes+dominated, **with a 2026-07-11 exemption: a goal-achieving acquiring candidate is never pruned for losing to a cheaper no-new-card option** — scoped to goal-achieving only, acquiring-vs-acquiring dominance unaffected) → hard rules (`misses_goal` never #1) → 6 config-weighted sub-scores (`config/ranking-weights-v1.yaml`, versioned) → composite → near-tie co-recommendation + headline differentiator; `reconcile_claim` re-labels with simulated truth, >10% gap warns. **`acquirable` card flag** (domain + seeds + migration 0002 + store + admin): Atlas filtered from acquisition in 3 independent places, still usable in-wallet. End-to-end integration suite (`tests/integration/test_optimization_pipeline.py`). Decision log for the cost-tier fix: `2026-07-11-cheapest-viable-archetype-and-dominance-fix.md`.

- **AI Reasoning Engine (build-plan Phase 5, test-first):** `app/ai_reasoning/` (Stages 1 & 10 — the ONLY LLM code), decision log `2026-07-05-phase5-ai-reasoning-engine.md`. `model.py` — the `ChatModel` protocol (one `complete()` call) + `model_from_settings` (returns None with no key → the whole engine runs on non-LLM paths). `_pydanticai.py` — the sole `pydantic_ai` import, lazy adapter (untested by design; live-key smoke test later). `intent.py` (Stage 1) — `extract_intent`: LLM proposes fields → deterministic re-validation against `CITY_TO_REGION`/`CabinClass` vocabulary (out-of-vocab → `missing_field`, never passthrough) → `ClarificationRequest`/`ScopeRefusal`; origin-from-profile is the only permitted default (flagged `assumed`); prompt carries vocabulary NAMES only, never ratios. `narration.py` (Stage 10) — `narrate`: deterministic `NarrationPayload` of finished numbers+names → LLM phrases → **number-echo validation** (every integer & catalog card name must trace to the payload; decimal-glued fabrications rejected) → ONE regeneration → **template fallback** (`model_version="template-fallback"`, ships real numbers, infeasible path narrates the adjustment menu). Boundary: `ai_reasoning→knowledge` sanctioned but narrowed to `goal_resolution` vocabulary only (DB reader forbidden, CI-enforced).

- **Pipeline Orchestrator + API v1 (build-plan Phase 6, test-first):** decision log `2026-07-05-phase6-pipeline-and-api.md`. `pipeline/context.py` (Stage 4) — `assemble_context`: flagged default spend template (`assumed=True`), empty wallet is valid, `horizon_months` rounds partial month UP / floors at 1 (golden-tested). `pipeline/run.py` — the orchestrator: `run_goal_pipeline` (Flow A: text/intent → package) + `run_from_context` (Flow B re-entry, Stages 5–11), composing all 11 stages linearly (no framework, blueprint §0.2); explicit early-exit union (`ClarificationNeeded`/`RouteUnsupported`/`ScopeRefused`), infeasible → `recommended=None` + adjustment-menu narration; client-held clarification loop (pass `intent` to skip Stage 1). `pipeline/assemble.py` (Stage 11) — `FinalRecommendation` with lineage stamp, `assumed_flags`, and computed `risks_and_limitations` (saver-availability caveat, tight/infeasible/stale-chart). `repositories/results.py` — the persistence seam (goal→simulation→result→recommendation FK chain, one txn, D-2 lineage) kept OFF the pure path; per-month-average columns; **not wired live until auth (Phase 7)** because `user_goals.user_id` FKs `auth.users`. `app/api/` — v1 surface: `/goals/parse` (Stage 1), `/goals/recommendation` + public anonymous `/simulations` (shared pipeline path, `_run_and_respond`), `/catalog/cards` (snapshot read, `acquirable` flag), `/health` (real snapshot version, DB-hiccup → null not 500); process-cached snapshot in `deps.py`. **The byte-identical determinism test is standing** (build rule 8: `run_from_context` whole-object identical; `run_goal_pipeline` identical modulo the minted goal id). Reviewer fixed 3 findings (FK-violation → persist deferred, monthly-column semantics, seam test coverage + confidence quantize). **215/215 total, mypy strict + ruff green.** `config.py` gained `llm_model`; `pydantic-ai-slim[openai,google]` added.

- **Auth + persistence + DB config for Phase 7 (Integration), live:** decision logs `2026-07-06-phase7-auth-persistence-live.md`, `...-db-pooler-fix-and-catalog-rls.md`, `...-richer-ui-and-auth-groundwork.md`, `...-simulator-live-integration.md`. **`api/auth.py`** — Supabase HS256 JWT verification (`require_user`): fails closed, `algorithms=["HS256"]` blocks `alg:none`/confusion, `options={"require":["exp","aud","sub"]}`; yields the real `auth.users` UUID. **`POST /goals/recommendation/save`** (`Depends(require_user)`, `persist=True`) writes the lineage chain under the verified user and returns **`persisted: bool`** (best-effort write reported honestly); anonymous `/recommendation` + `/simulations` stay `persist=False`/`persisted=null`. **CORS** `allow_headers=["Content-Type","Authorization"]` (the Bearer save survives preflight), no wildcard/credentials, config-driven origins. **DB engine** (`deps.py`): transaction-pooler URL + `statement_cache_size=0` + `NullPool` + `pool_pre_ping` + `timeout=5` — the required combo for Supabase PgBouncer (else `DuplicatePreparedStatementError`/`ConnectionDoesNotExistError`); mirrored in `alembic/env.py`. **`MonthLedgerEntry.points_earned_this_month`** — deterministic per-month earn delta (TDD golden) so the chart is a clean monotonic curve. Migrations **`0003`** (catalog RLS + public-read policies) and **`0004`** (`auth.users → public.users` sync trigger, `SECURITY DEFINER`, `REVOKE EXECUTE FROM PUBLIC`, backfill) applied to the live DB. `pyjwt` added; `.env.example` documents the pooler URL + `SUPABASE_JWT_SECRET` + `CORS_ALLOW_ORIGINS`. Reviewers fixed: chart-math bug, JWT `exp` gap, CORS `Authorization` header, honest `persisted` reporting. **240 total green, mypy strict + ruff clean.**

- **Strategy "story" output (post-Phase-7, 2026-07-11):** the recommendation is now explainable as a narrative, not a single thin pick. **Per-category earn detail** — new frozen `StrategyAllocationDetail` (`domain/strategy.py`) built by `optimization/explain.py` (`allocation_detail` joins each category to its Stage-5 `RewardOpportunity`: earn rate, effective miles/₹100, notes); `rank()` gained an optional `opportunities` param that attaches `allocation_details` to each `RankedStrategy` (threaded from `pipeline/run.py`; omitted in focused unit tests, back-compatible). **Per-category `monthly_points` is illustrative (per-category floor, documented not-summable); the honest per-card total is `card_monthly_points()` — exact cross-category sum floored ONCE, matching `allocation.py`/`projector.py`** (the frontend mirrors this, computing the card header from spend×rate not by summing rows). **Persistence (no migration):** `results.py` writes `allocation_details` + `score_breakdown` + `headline_differentiator` + a compact `strategy_options` tier list into the existing `card_allocations` JSONB; `SavedStrategy`/`SavedGoalDetail` + `detail_from_row` reconstruct them (default empty on older saves). **Narration** now uses the previously-dead `alternatives`: `NarrationPayload.comparison` + `_template` populates `comparison_notes` deterministically, prompt enriched, all tier numbers echo-allowed, every prior contract preserved. Live-DB save→read round-trip verified (rolled back). 279 tests, mypy strict + ruff clean. Decision log: `2026-07-11-strategy-story-output.md`.
- **Saved-goal delete (post-Phase-7):** **`DELETE /goals/{id}`** (`Depends(require_user)`, 204/404) → `delete_goal_lineage` in `repositories/results.py` (stays the sole writer of user-result tables): one transaction, child-first `recommendation_outputs → simulation_results → spend_simulations → user_goals`, every statement bound to both ids (`simulation_results` scopes via `USING user_goals` — no `user_id` column). `simulation_results` is deleted explicitly, NOT left to the `spend_simulations` cascade: its `goal_id` FK is `NO ACTION` and the cascade only covers it while result-goal ids happen to match (unenforced invariant). Live-DB verified in a rolled-back txn. Decision log: `2026-07-10-dashboard-polish-and-goal-delete.md`.
- **Saved-goals read API (post-Phase-7):** **`GET /goals`** (list) + **`GET /goals/{id}`** (detail), both `Depends(require_user)`, backed by read repo **`repositories/saved_goals.py`** (`list_saved_goals` + `get_saved_goal`: parameterized, user-scoped, `LEFT JOIN LATERAL` deterministic latest-recommendation pick with `ORDER BY created_at DESC, id DESC`; detail additionally LEFT JOINs the recommendation's `simulation_results` row and is scoped to BOTH `g.id = :goal_id AND g.user_id = :user_id` — unknown id and foreign goal are the same 404). Detail response `SavedGoalDetail` (schemas.py) is **pure reconstruction of the persisted JSONB** (spend allocation, cards used/to-acquire, month ledger, milestones, transfer plan, action items incl. `card_id`, priority-sorted) via `detail_from_row` in `api/goals.py` — never recomputed (D-2: reopening a goal shows exactly the save-time artifacts, snapshot/engine version included). `display_names()` adds cosmetic `card_names`/`partner_names` maps from the *current* snapshot (retired ids omitted, client falls back). Infeasible goals: `strategy=None`, narration still ships. 16 detail tests (repo fakes + mapping + 401/404) + live-DB read verified (real goals reconstruct; cross-user returns None). backend-reviewer: no crit/important (scoping airtight by construction).
- **LLM via OpenRouter:** `config.py` gains `llm_base_url` (empty ⇒ real OpenAI); `_pydanticai.py`'s openai branch passes it when set — OpenRouter is just openai+base_url+key+model, no new provider enum. `.env`/`.env.example` document the pattern + free Qwen model. Wiring proven live; free tier is upstream-rate-limited (429s).
- **Runner-up cause attribution (2026-07-13):** when a category's runner-up rates strictly higher than the chosen card, `optimization/explain.py` runs the real counterfactual (`claimed_estimate` with just that category swapped) and attributes the verified whole-plan delta on `StrategyAllocationDetail.runner_up_reason`/`runner_up_plan_delta_miles`: `transfer_cap` (via the new engine-owned `ClaimedEstimate.cap_bound_currencies`, set exactly where the annual link cap clamps — never inferred from the block-floored plan), `milestone` (lost-milestone set difference; presence-not-magnitude, documented), `fewer_total`, `equal_total`, `route_shape` (ONLY for forced single-card archetypes, whose counterfactual also uses their own `include_idle_balances=False` basis; a gaining swap on a hill-climbed route ships no cause — search artifact, not design). Threaded via `ranking._details_for`; persisted through the existing JSONB (`SavedAllocationDetail` widened, `str`-typed reason for forward-compat); `runnerUpSentence` states each cause with the verified number, null falls back to the old neutral sentence. 8 golden tests over the real seed catalog (hand-derivations in `test_explain.py`); recurring-milestone React key fixed (`id+month`). Decision log `2026-07-13-runner-up-cause-attribution.md`.
- **Infeasible → best-effort plan (2026-07-12, 3rd sitting):** Stages 7–9 no longer gated on `verdict.feasible` (`pipeline/run.py`; `generate_candidates` drops its infeasible⇒() short-circuit — goal-clearing archetypes naturally emit nothing, so every candidate honestly misses and ranking's `misses_goal` hard rule picks the least-bad as the best-effort recommendation; () only when nothing is allocatable, e.g. cashback-only wallet + no_new_cards, which keeps the menu-only response). Narration payload builds adjustment notes whenever infeasible; prompt + template gained an infeasible-with-plan branch ("the best route still earns X of the Y miles needed" + menu); success-framed `comparison_notes` suppressed when infeasible (reviewer catch). Persistence unchanged structurally (`recommended is not None` was already the branch) — `goal_feasibility` type now legitimately co-occurs with a result row + confidence; **the Stage-6 adjustment menu persists** (`adjustment_options` in `card_allocations` JSONB → `SavedGoalDetail.adjustment_options`). UI: `VerdictHero.bestEffort` ("…but the best route still gets you ~P" + gap line, close-the-gap clause gated on `hasAdjustments`), shared `AdjustmentMenu` between hero and tabs (live + saved + simulator fallback), saved view derives feasibility from `recommendation_type` not strategy presence, `tradeOff()` and the book step no longer lie for goal-missing tiers (reviewer catches). Decision log `2026-07-12-infeasible-best-effort-plan.md`. 305 tests.
- **Guided-flow slice 10 backend — LLM education framing (2026-07-15, decision 10):** `app/ai_reasoning/education.py` — `narrate_education(EducationFacts, model, timeout_seconds=10)`: same sandwich as Stage 10 (facts in → one constrained call → number-echo check), plus a per-call `asyncio.wait_for` bound so a slow/429ing model can never hang the request (verified live: flaky OpenRouter key → `{"narrative": null}` in ~14.5s incl. cold snapshot load); integer allowlist + verbatim decimal-token allowlist (non-integral earn rates like 16.65 echo exactly or not at all — "16.7" can't launder via "16") + catalog-name hallucination guard; None (not a template) is the fallback — the wizard's deterministic education render IS the template. `EducationFacts` is self-contained (ai_reasoning must not import knowledge.education — boundary held). `GET /catalog/education/story` (api/catalog.py) + `education_facts()` builder whose allowlists are test-proven to back every number its own fact lines state — the self-consistency sweep runs over the FULL catalog and immediately caught a real gap (Magnus Burgundy's category label embeds "tiered 35/₹200 above ₹1.5L" figures the field-by-field adds never saw; fixed structurally: every finished fact line is swept and its tokens allow-listed verbatim, mirroring narration.py's embedded-text rule); quarterly/annual caps render alongside monthly (reviewer polish). backend-reviewer: zero critical/important; all polish applied. 11 contract tests + 2 API tests.
- **Guided-flow slice 8 — the acquisition pair (2026-07-15, decisions 8–9):** `config/ranking-weights-v2.yaml` (v1 base weights unchanged + new `acquisition_weights` profile: low fees → cost 30, ecosystem → efficiency 30, transfer-quality → risk 20, goal 15, simplicity 5, portfolio 0 with rationale comments; app default now points at v2). `optimization/ranking.py`: `AcquisitionWeights` base class (`RankingWeights` subclasses it, gains optional `acquisition`), `select_acquisition_pair` — **cheapest picked by lowest `card_fees_inr` (ties→rank), NOT by archetype label** (Stage 9's duplicate-prune can merge the forced cheapest_viable plan into an identical one_new_card plan — the fee is the fact, the label isn't); best_value = top acquisition-composite among achieving `one_new_card` (Stage-9 composite formula, different weights); goal-missing candidates never offered; same acquisition set collapses to one `cheapest_and_best_value` entry (higher composite wins) — and `select_guided_routes` (None when the wallet clears or nothing acquiring achieves → existing presentation; else pair in rank order + wallet best-effort last). `RankedStrategy.acquisition_role` (additive, persisted through `strategy_options` JSONB + `SavedStrategyOption`; typed in `api.ts` for slice 9). Hand-computed composite goldens + seed-catalog end-to-end + an API-level test through the real `get_weights()` default (backend-reviewer's ask: the v2 default deliberately changes route composition for the LIVE one-shot flow too — that IS the slice; pinned by test). Live: empty wallet 12mo → best_value Magnus (₹30,000, 121,440mi) + cheapest DCB (₹10,000, 84,249mi).
- **Guided-flow backend slices 1–3 (2026-07-14, design log `2026-07-13-guided-strategy-flow-design.md`):** (1) **Total-spend budget** — `RecommendationRequest.total_spend_inr` (gt=0, model-validator 422 when sent with `spend_profile`); `pipeline/context.py` gains `spend_profile_from_total`: `monthly = floor(total ÷ horizon)`, each template category `floor(monthly × weight ÷ 90k)` (weights = the default template), zero-floor categories dropped, tiny totals honestly yield an empty profile, always `assumed=True`; threaded via `run_goal_pipeline → assemble_context` (mutually exclusive, ValueError on both). Hand-computed goldens incl. non-divisible ₹1L/7mo. (2) **Education endpoint** — `app/knowledge/education.py` `wallet_education()` (pure snapshot reshape, frozen models, deterministic ordering, `UnknownCardId` fails loud) + `GET /catalog/education?card_ids=…` (404 unknown id, 422 empty); per card: currency, earn rules with labels/caps, transfer links with ratio/cap/fee/processing, plus partners shared across ≥2 wallet cards (Atlas+TravelOne → KrisFlyer verified live). (3) **Feasibility probe** — `run.py` Stage 1–4 prefix extracted into `_prepare_context` (behavior-preserving, reviewer-traced); new `run_feasibility_probe` runs Stages 1–6 only (spy test asserts Stage 7+ never runs) returning `FeasibilityProbe` (verdict + adjustment menu + requirement + lineage, no minted ids ⇒ repeat-identical); `POST /simulations/probe` (36ms live on the hopeless 2-pax capped-Atlas case, menu returned). backend-reviewer: no critical/important findings; its 3 polish notes applied (docstring, guard tests, probe spy).
- **Route selection + fee split + earn story (2026-07-12):** `select_route_options()` (`optimization/ranking.py`) caps the package at ≤3 routes, one per distinct acquisition set, wired through `pipeline/run.py` so narration/assembly/persistence all see the same selection (original `rank` values preserved, documented). `SimulationOutcome` split: `card_fees_inr` + `transfer_fees_inr` (projector computes; `total_fees_inr` = sum, still what Ranking's cost dimension scores); narration payload/template narrate card fees only ("New-card fees ₹30,000" — totals no longer echo-allowed). `StrategyAllocationDetail` enriched (all optional, older saves deserialize): `monthly_miles`, `currency_name`, `transfer_ratio_from/to`, `category_label` (accelerated-rule label from the catalog), `runner_up_card_id`/`runner_up_miles_per_100inr` (best other card available in the plan per category) — built in `explain.py` as a pure Stage-5 reshape, threaded via `_story_inputs` in `rank()`. `display_names()` (api/goals.py) maps ids across ALL strategy_options tiers + runner-ups (fixes "Card no longer listed" on alternative tabs). Per-row transfer-fee valuation note removed from `opportunities.py` (fee is on `transfer_path` + `transfer_fees_inr`; UI shows it once). Decision log `2026-07-12-route-selection-fee-split-and-earn-story.md`.

### In progress
- Nothing active.

### Next up
- **Guided strategy-flow revamp (design first; user expanded the spend-model revamp into a full flow, 2026-07-13):** the user's target flow — (1) goal + **approx total spend over the horizon** (e.g. ₹6,00,000 over 12 months; replaces upfront monthly category inputs); (2) **card + ecosystem education step**: describe the selected cards, their transfer partners (e.g. both Atlas and TravelOne → KrisFlyer), and the reward system with concrete rates (TravelOne 2 pts/₹100 base, 4 travel, 16/24 via HSBC portal for flights/hotels, 1:1 to KrisFlyer); (3) ask "want a spending strategy?" → if yes, category-wise split of the total over the chosen horizon; (4) full visible calculation **with per-category reasoning** (why Atlas wins here, TravelOne there — runner-up attribution already shipped serves this), LLM framing via a good system prompt (number-echo validation already in place); (5) **feasibility check at the END** with confidence if yes; if no, offer strategies with extra cards — 2 alternatives; (6) **extra-card selection algorithm** scoring low fees + reward ecosystem + transfer-partner quality (ease-to-get explicitly out of scope). **Design LOCKED via grilling session 2026-07-13 — decision log `2026-07-13-guided-strategy-flow-design.md` (13 decisions: one wizard everywhere, total-only input with floor-monthly spread + visible even-spend footnote, server-side total→profile split to keep the `assumed` flag honest, new education endpoint as a pure catalog read, silent early feasibility probe interrupting only hopeless goals, declined-split still yields a full assumed-split strategy, categories-win split UI, feasible=hero+quiet upgrade tabs / infeasible=explicit ask → cheapest_viable + best one_new_card pair, acquisition criteria as a versioned ranking-config weights profile, LLM framing never blocking, conversational-scroll wizard, new-card toggle dropped, empty wallet folds education into the strategy output).** **Sliced 2026-07-13 into 10 dependency-ordered vertical slices, appended to the decision doc** (§ Implementation slices): 1 total-spend request field → 2 education endpoint → 3 feasibility-probe endpoint → 4 wizard shell → 5 early-stop for hopeless goals → 6 education step → 7 opt-in split step → 8 acquisition pair (cheapest + best-value, weights profile) → 9 end-of-flow verdict/extra-card ask → 10 homepage swap + LLM framing + reviewer pass. **ALL 10 SLICES SHIPPED** (1–3 on 2026-07-14; 4–10 on 2026-07-15 — see Done sections). Remaining follow-ups from the revamp: (a) **seed the HSBC-portal accelerated-rate rows** (the "16/24 via HSBC portal" canonical example — line-by-line review bar; the education endpoint/step will surface them automatically once seeded); (b) **saved-goal signed-in surfaces live click-through** (still reviewed statically only — now also covers the wizard at `/goals/new` and saved views of pair-labeled strategies); (c) `/simulations` latency (40s measured again this session, see the standing item below) is now the wizard's PlaneLoader wait — unchanged cause (narration LLM retry), higher UX stakes.
- **Strategy "story" output — DONE this session (2026-07-11).** The vague-explanation concern is resolved: per-category earn rates + points math, the "your cards → +1 → +2" tier comparison, and `score_breakdown`/`headline_differentiator`/`strategy_options` are now persisted (in the `card_allocations` JSONB, no migration) and rendered in both the live simulator and saved-goal views. The "selected card excluded from allocation" observation was diagnosed as correct optimizer behaviour (one strong card genuinely wins every category — miles-optimal), now made legible by showing the per-category *rates*. Decision log `2026-07-11-strategy-story-output.md`. Remaining follow-up: forcing multi-card diversification is deliberately NOT done (would recommend fewer miles).
- **Snapshot-load resilience (noted follow-up):** the catalog snapshot loads once and caches; a transient cold-pooler refusal at the first request leaves the process serving `null`/500 until restart. Add a retry so a single refusal doesn't poison the process. (Non-blocking; DB is reliably up on retry.)
- **Automated real-DB round-trip test** for `persist_recommendation` — persistence is proven manually against the live DB; an integration test needs a real `auth.users`-backed user + a test DB. The unit tests (capturing fake conn) still cover the seam's shape.
- **LLM is wired (OpenRouter/Qwen) but the free tier is rate-limited:** `.env` routes through OpenRouter (`llm_base_url=https://openrouter.ai/api/v1`) at `qwen/qwen3-next-80b-a3b-instruct:free`. Wiring is proven live, but the free tier caps ~50 req/day + upstream provider throttling → intermittent 429s, so narration/free-text is unreliable. **To make it usable:** buy $10 OpenRouter credits once (free-model daily cap 50 → 1000, credits stay unspent) or point `LLM_MODEL` at a paid model. Note the Nemotron *content-safety* model originally suggested is a classifier and can't do intent/narration — must be a general instruct model.
- **USER (security):** **rotate the OpenRouter key** (pasted in chat this session — exposed). Also rotate the Supabase DB password when convenient (transited chat during pooler debugging). Publish the Google OAuth consent screen before launch (currently "Testing" — only added test users can log in; non-sensitive scopes ⇒ no Google review needed).
- **D-5 two-part streaming response** — deferred (decision log): narration currently ships inside the single `FinalRecommendation`; revisit streaming when a live LLM key is added and narration latency is measured against the 30s budget.
- Remaining `needs_verification: true` seed rows (`grep -rn "needs_verification: true" backend/seeds/catalog`): all 5 award-chart rows (45,000-vs-46,000 source conflict; SIA PDF unreachable), Axis fee/min-transfer, Burgundy earn table post-Apr-2026, HSBC min-transfers, Amex Plat Charge details, Regalia base rate, Infinia/DCB SmartBuy cap unit. (Live catalog was reloaded 2026-07-05 with migration 0002 + the expanded/acquirable-flagged seeds; round-trip verified.)
- BR-05/BR-06 (milestone validity windows): currently guarded against in `validate_catalog()`; implementing them needs a calendar anchor (plan-start date) in the simulation inputs — remove the guard in the same change.
- DP-07 preference-aware ranking-weight modulation: `config/ranking-weights-v1.yaml` supports it; wire once the preference-collecting Goal Discovery UX exists.
- (Optional) mvp-scope-v2.md card-list amendment pass; Maharaja Club link expansion (HSBC 1:1, Regalia 100:33 documented but not yet seeded); Standard Magnus Group A annual cap + Maharaja award rows.
- (IDE nicety) Point the editor's Python interpreter at `backend/.venv/bin/python` — the root `.venv` is empty and triggers false "package not installed" hints.

---

## Frontend

### Done
- **Product-first, edge-to-edge landing page** (`src/app/page.tsx`): Goal Simulator is section 2 directly under the hero; then outcomes / how-it-works / trust pillars / strategy output / cards / ecosystem marquee / built-for / capabilities / mission / FAQ / CTA. True full-bleed (gutters only on `Inner`), dark/gold design system. Layout primitives in `section-shell.tsx` (`Bleed`/`Inner`/`Measure`). Decision: `2026-06-26-landing-page-product-first-fullbleed.md`.
- **Motion:** Framer Motion v12 (`motion`) is a real dependency. Hero pin-and-release is driven off raw document `scrollY` in pixels (clamped px thresholds) — scoped `useScroll({target})` and document-fraction variants were both buggy; don't reintroduce them. Simulator section is a normal scroll block with one-time `whileInView` entrance (a pinned ~260vh version was reverted — hijacked the wheel). All effects collapse under `prefers-reduced-motion`. CSS/IO primitives (`Reveal`, `CountUp`) coexist.
- **Supported cards**: static responsive grid (5/3/2-up, no carousel, no crop — `object-contain` on dark plates), 5-card illustrative wallet with real photos (Infinia, DCB, Regalia Gold, TravelOne, Amex Platinum Travel). SBI removed from UI only (still in product scope). Decision: `2026-06-21-supported-cards-photos-and-scope.md`.
- **Goal entry is LIVE against the backend** (now `goal-wizard.tsx` — the one-shot `goal-simulator.tsx` was superseded and deleted 2026-07-15, see the wizard bullet above): posts a structured intent to `POST /simulations` through a typed client (`src/lib/api.ts`, base URL via `NEXT_PUBLIC_API_BASE_URL`) and renders the real response union (recommendation / clarification / unsupported-route / infeasible-adjustments) with loading + error states. _(Historical detail from the one-shot era, wiring unchanged:)_ **Goal entry is a sentence-form (2026-07-11 revamp):** "I want to fly [cabin] to [dest] from [origin] within [N] months, for [n passengers]" as inline controls, plus per-category monthly spend inputs (travel/dining/online/groceries/utilities, live total; empty ⇒ engine's flagged default template — the hidden `SPEND_MIX` split is gone), an "Open to adding a new card?" preference (client-side: default-selects the no-new-cards route tab; DP-07 server-side wiring deferred), and a wallet picker listing the FULL catalog (`acquirable` gates new-card proposals, not what you can hold — Atlas is holdable). Decision logs `2026-07-06-phase7-simulator-live-integration.md`, `2026-07-11-strategy-ux-revamp-narrative-flow.md`.
- **Strategy story v3 (2026-07-12, 2nd sitting):** typography bumped one step across the whole narrative (hero headline text-2xl/3xl, body text-base, notes text-sm; FinePrint stays small) and SpendStep's per-card math is a real `<table>` (6 columns + Total row rendering only with >1 categories; spend sum + floor-once `cardMonthlyPoints` total; `overflow-x-auto min-w-[560px]` so it scrolls internally on mobile — verified live, zero page overflow), with per-category guidance (gold "To get this rate", runner-up sentence, unique notes) in a block below the table; note hoisting + all-default-rate collapse preserved. Reviewer polish follow-up (optional): a scroll-affordance fade on the table wrapper; runner-up sentence repeats verbatim per category on 2-card plans (pre-existing).
- **Strategy story v2 (2026-07-12):** route tabs show ≤3 distinct tiers with real card names and card-fee labels ("₹0 card fees"); SpendStep tells the full earn math per card — reward-system subline ("Earns EDGE Miles · transfers to KrisFlyer at 1:2"), per-row rate chain ("2 pts per ₹100 → 4 miles/₹100"), worked example ("₹20,000 × 2/₹100 = 400 pts → ~800 KrisFlyer miles a month"), gold "To get this rate: Travel via SmartBuy portal" line, runner-up comparison sentence (cause-neutral phrasing), card-level note hoisting (shared notes once per card; all-default-rate rows collapse to one sentence); transfer step footnotes the bank's program fee once; feasible VerdictHero no longer repeats the narration summary. `PlanTier` gains `transferFees`; saved tiers fall back `card_fees_inr ?? total_fees_inr` for older saves. frontend-reviewer verified live at both viewports.
- **Strategy result is a narrative (2026-07-11 revamp, decision log `2026-07-11-strategy-ux-revamp-narrative-flow.md`):** shared system in `components/strategy-story.tsx` — `VerdictHero` (plain-English verdict first: "~51,282 KrisFlyer miles by month 7 — a month ahead", infeasible variant + adjustment menu), `StrategyPlanTabs` (route options as tab-cards; **selecting one swaps the entire plan** — split/transfers/chart/why — using the per-tier `allocation_details`/`transfer_plan`/ledger the API already returned; one-sentence trade-off vs. the recommended route), `PlanSteps` (numbered: get card → route spend [per-card groups, valuation notes inline, milestone bonuses] → transfer [months + processing note] → book [chart miles/seat; buffer is ON TOP of `miles_required_total`, verified live]), `EarnChart` (goal-month dashed marker + clamped "goal" pill, honest window subtitle), `WhyThisRoute` (plain-language reasons; the raw 6-dimension bars demoted to a "How OptiMiles scored this route" disclosure that explains set-relative semantics), `NextSteps`, `FinePrint` (human-first lineage). Consumed by both `strategy-detail.tsx` (live; every tier full) and `app/saved-strategy.tsx` (persisted; recommended tier full, alternatives get an honest compact panel; older saves synthesize one tier; horizon from ledger length). `api.ts` gained `CandidateStrategy.transfer_plan` + `expected_milestones`. frontend-reviewer drove it live at 1440×900 + 390×844 across four scenarios: zero console errors, no overflow; its 3 findings applied (mobile spend-row truncation → wrapping rows, goal-pill overflow → CSS clamp, `acquirable` picker bug). **2026-07-11 (4th sitting): verified live with a real 4-tab response (new `cheapest_viable` backend tier) — the tab grid, per-tab plan-swap, and trade-off sentence all handled 4 tiers correctly with zero code changes** (tabs already build generically from the response array); one legibility gap found and fixed — `tierLabel` now appends the engine's `headline_differentiator` in parentheses when two tiers converge on the same card label (e.g. two different archetypes both landing on "Add Magnus for Burgundy"), threaded via a new `PlanTier.headline` field from both `RankedStrategy` (live) and `SavedStrategyOption` (saved goals). tsc + eslint clean.
- **Supabase auth is LIVE** (`lib/supabase.ts` PKCE client, `auth-form.tsx`, `app/auth/callback/page.tsx`, `lib/use-auth.ts`, `site-nav.tsx`): **Google** sign-in (Apple removed — needs a paid Apple account) + email/password; `/auth/callback` exchanges the OAuth `?code=` for a session (the fix for "came back logged-out"); `useAuth()` subscribes to the session so the **nav shows the user's email + Sign out** live. Keys are in `frontend/.env.local` (gitignored). Verified end-to-end with a real Google account.
- **"Save this goal" is LIVE** (`goal-simulator.tsx`): logged-in users get a Save button → `simulateAndSave(request, token)` → `/goals/recommendation/save` → the goal persists to the live DB (proven). The button trusts the server's `persisted` flag (only "Saved ✓" when the row actually landed), waits on `useAuth().loading` to avoid a logged-out flash, and shows a distinct "session expired" message. Logged-out users see a "Log in to save" prompt.
- **Signed-in app shell + dashboard (the post-login product surface, 2026-07-10):** route group **`src/app/(app)/`** wraps `/goals`, `/goals/new`, `/goals/[id]`, `/cards` in one shared client shell (**`components/app/app-shell.tsx`**): fixed left sidebar (`w-64`, 15px nav text/18px icons; Brand; Dashboard / New goal / Supported cards nav with active-state matchers that keep Dashboard lit on goal-detail routes; bottom user chip + sign-out), sticky top bar (route-derived serif title `text-lg sm:text-xl` + gold "+ New goal", default size), mobile drawer, and the auth gate (children render only with a session; else redirect to `/login`). **Dashboard home** (`(app)/goals/page.tsx`), layout per user feedback 2026-07-10: 3 stat tiles on top (goals count / total miles targeted / nearest target date — pure reads of persisted rows, large tabular-nums values with unit suffixes), focus-goal hero below them (newest goal: months-to-target, confidence bar, View strategy), then an "All goals" grid showing EVERY goal (1/2/3 cols at base/sm/xl — base `grid-cols-1` is load-bearing, without it narrow viewports overflow). Each card: stretched-link navigation + three-dot menu (`GoalMenu`: View strategy / Download strategy JSON via `fetchSavedGoal` blob / Delete → opens the centered `ConfirmDialog` modal → `deleteSavedGoal` → `DELETE /goals/{id}`, removing the card from local state; the old cramped in-menu two-step confirm was replaced 2026-07-11). Goal-first empty state. **Goal detail** (`(app)/goals/[id]/page.tsx` + `components/app/saved-strategy.tsx`): the stored strategy rendered in the live simulator's visual language — accumulation SVG chart from the persisted ledger, spend routing, cards-to-add, transfer plan (partner names from the response's name maps), milestones, action items, reasoning, snapshot/engine lineage footer. **In-app goal creation** (`(app)/goals/new/page.tsx`) embeds `GoalSimulator` unchanged; its "Saved" state links back to the dashboard. **`/cards`** lists the catalog grouped by bank ("Existing holders" tag for non-acquirable). Marketing nav label "My goals" → "Dashboard". frontend-reviewer drove a real browser (desktop 1440×900 + mobile 390×844): no overflow, no console errors, auth-gate redirects verified on all four routes; its one important finding (sidebar active-state lost on `/goals/[id]`) fixed.
- **Shared UX primitives (2026-07-11):** `components/ui/confirm-dialog.tsx` — reusable portal modal (dimmed backdrop, focus-on-open + Tab focus-trap between its two buttons, Escape/backdrop dismiss, body-scroll lock, busy state that blocks dismissal + double-fire, destructive variant, in-dialog error). `components/ui/plane-loader.tsx` — branded loading animation (self-contained SVG flight arc + `offset-path` plane + motion trail + cycling stage copy, reduced-motion fallback to a static arc). Both used across the delete flow, the two authed loading skeletons, and the simulator's strategy-generation wait. tsc + eslint clean; frontend-reviewer verified live. **Known constraint: `offset-path` is Chromium-only — the plane renders static (acceptable) on Safari.**
- **Storybook + design-sync:** Storybook 10.4.6 (`@storybook/nextjs-vite`), 28 stories; 6 components synced to claude.ai/design ("OptiMiles Design System") via the `src/lib-entry.ts` export surface. Decisions: `2026-06-27-storybook-setup-for-design-sync.md`, `2026-06-27-design-sync-claude-design-import.md`.
- Brand/chrome: OptiMiles rebrand, named-partner copy stripped, dark native controls (`color-scheme: dark`), `SiteNav`/`SiteFooter`/`Brand`, `/login`+`/signup` with shared `AuthForm`. Build + eslint clean; footer polish verified in-browser via Playwright.
- **Guided wizard COMPLETE — slices 5–7, 9, 10 (2026-07-15, 2nd sitting):** the wizard is now THE goal-entry surface (decision 1): the homepage's pinned scene and `/goals/new` both embed `GoalWizard`; `goal-simulator.tsx` (the one-shot) is DELETED — its result rendering moved to `wizard/result.tsx` (`SimulatorResult`/`SaveGoal`), `InlineSelect` into the wizard. **Slice 5:** budget-step Continue fires the silent probe; clearly-hopeless → inline "This goal isn't reachable as stated" panel (best-case vs required + shared `AdjustmentMenu` + steps stay editable; clearing on any edit/total change); probe failures pass silently (never block). **Slice 6:** "How your cards earn" step (`wizard/education-step.tsx`) — shared-partner gold callouts ("2 of your cards … KrisFlyer"), per-card earn rules with labels/caps and transfer links with ratio/cap/fee/days, all deterministic template text; empty wallet skips the step with the one-liner and the strategy output leads with "Meet your recommended card(s)" education for the acquisitions (decision 13); LLM framing paragraph lands on top when `/catalog/education/story` returns one (never blocks). **Slice 7:** "Want a spending strategy?" (`wizard/split-step.tsx`) — Skip runs on the assumed split with a visible caveat + one-click "Refine the split →" reopening the engaged editor; Yes opens categories-win rows pre-filled from the 25/20/20/15/10 template scaled to their total, edits free, Total re-derives (no equality validation); engaged split sends `spend_profile` (monthly floors), never `total_spend_inr`. **Slice 9:** wallet-can't-clear responses render `WalletShortfallHero` + the wallet-only plan + `ExtraCardAsk`; reveal remounts the tabs (distinct keys — reviewer-caught stale-selection bug fixed) selecting the recommended labeled route, gold "Lowest fees"/"Best value" badges, `NextSteps` only post-reveal; feasible-wallet runs title acquisition alternatives "Optional upgrades — add a card to go further"; empty wallet shows badges with no ask. Three frontend-reviewer live passes this sitting (homepage + wizard at 1440×900/390×844, payloads network-verified, zero overflow/console errors); the one critical finding (reveal not switching selection) fixed and re-verified.
- **Guided wizard shell (2026-07-15, slice 4 — decisions 1, 2, 11, 12):** new `components/goal-wizard.tsx`, embedded at `/goals/new` (the homepage deliberately keeps the one-shot simulator until slices 6–7 land; slice 10 swaps it everywhere and deletes the one-shot). Conversational scroll: goal sentence → bank-grouped card picker (flagships pre-selected, empty wallet allowed with reassuring copy) → ONE total-over-horizon input with the live "≈ ₹X/month — we assume even spending" footnote (floor division; blank ⇒ engine's assumed default template) → strategy step (PlaneLoader → full `StrategyDetail`). Completed steps collapse to editable summary lines (check dot + Edit); editing invalidates the result but keeps inputs; auto-scroll to the active step fires only post-interaction (fresh load never scrolls), reduced-motion honored. Per-category inputs and the "open to new cards?" toggle are GONE from the wizard (decision 12). Reuses `InlineSelect`/`SaveGoal`/`SimulatorResult` (now exported from `goal-simulator.tsx`). `api.ts`: `SimulateRequest.total_spend_inr` + `acquisition_role` typed on live and saved tiers. frontend-reviewer drove six scenarios live at 1440×900 + 390×844 (payload carries `total_spend_inr` and no `spend_profile`; edit-invalidation, empty-wallet, blank-total all correct; zero console errors, zero overflow, homepage regression-free). Temp public preview route used for the review was deleted after.
- **Goal-page UI/UX polish (2026-07-13):** simulator form typography bumped (goal sentence text-2xl/sm:text-3xl, section labels text-base font-semibold, spend rows 15px with h-10 inputs via `text-[15px]!`); wallet picker rebuilt as **bank-grouped tile sections** (catalog order, rounded-xl tiles with check-dot selected state, aria-pressed kept); **auto-scroll to the finished strategy** (`resultRef` on the aria-live container, `scroll-mt-24` clears the sticky top bar, fires only when `!loading && response`, honors reduced motion — reviewer-verified it doesn't yank mid-run); **save-in-progress state** (spinner in the button + role="status" "Recomputing and saving — up to a minute; keep this page open" — the save re-runs the full pipeline server-side, hence slow); dashboard **active-goal gold treatment** (hero border-gold/45 + gold glow + pulsing dot, "Active goal" eyebrow and dot gated on `goal.status === "active"` with a title-cased fallback — reviewer catch; grid status chips gold when active); `/goals/new` header bumped. frontend-reviewer drove the public simulator live at 1440×900 + 390×844: no overflow, no console errors, 15px override confirmed. Known trade-off (reviewer polish note, deliberately kept): the mobile goal sentence at text-2xl wraps ~5 lines.
- **Known constraint:** `lucide-react` pinned to v1.21 (`frontend/AGENTS.md`) — **no brand/logo icons exist in this version** (importing `Twitter`/`Github`/etc. crashes the dev server); use generic icons only.

### In progress
- Nothing active on auth/simulator — login + Save are live and reviewer-checked. Homepage still wants a human scroll-through beyond the hero (marquee, mobile nav rhythm); hero pin-and-release already user-confirmed.
- `testimonials.tsx` in-repo but unused (dropped from homepage, not deleted).
- **Flagged drift:** the `2026-06-21` redesign decision doc still says "dependency-free, no Framer Motion" — contradicted by the `2026-06-26` adoption; needs reconciling.

### Next up
- **Re-confirm the signed-in surfaces after the revamp** — one click-through behind Google OAuth: the dashboard (three-dot menu, delete modal, loaders) AND the saved-goal detail page now rendered by the new narrative components (the reviewer verified the new result UI only on the public simulator; the saved view was reviewed statically).
- **Saved-goal detail parity, part 2:** persist the *alternatives'* full plans (per-tier `allocation_details` + ledgers) and the feasibility verdict in `results.py`, so saved goals get the same full tab-swap the live simulator has (today alternatives show an honest compact panel). Score breakdown is already persisted.
- Free-text goal entry via `/goals/parse` in the UI — the sentence-form is the stopgap; flip to free-text-primary once a reliable LLM key/model is in place (free tier 429s).
- Publish the Google OAuth consent screen before launch (currently "Testing" — only added test users can log in).
- (Optional) Axis/Amex card photos for the wallet; real photos for `DreamOutcomes` placeholders; more components into design-sync if needed.
- Reconcile the "no animation library" claim in the `2026-06-21` decision doc.

---

## Docs / Product

### Done
- `/docs` fully structured (`prd`, `architecture`, `research`, `ux`, `decisions`, `prompts`) with a maintained decision log (see `docs/decisions/` — filenames are the index; latest: `2026-07-04-phase1-knowledge-engine-and-seed-catalog.md`).
- **Canonical doc set for the build:** `prd/mvp-scope-v2.md` (scope; v1 superseded-bannered) · `prd/optimiles-constitution-v1.md` (tie-breaker) · `architecture/system-execution-flow-v1.md` v1.1 (the 11-stage pipeline blueprint) · `architecture/backend-build-plan-v1.md` (**backend entry point** — D-1…D-7, schema v1.1, 8 phases, seed rules; declared the last planning doc) · `architecture/core-domain-model-v1.md` (vocabulary) · engine specs (`reward-knowledge-engine-spec`, `optimization-engine-spec`, `recommendation-engine-design`, `simulation-engine-spec-v1` — Phase 3 reference) · `architecture/db-schema-v1.md` (bannered: v1.1 amendment lives in the build plan §3).
- Notion corpus mirrored into `/docs` with provenance headers, engine docs consolidated onto the five-engine boundary, conflicts resolved (deterministic generation, bounded archetypes). Latest mirror pass (2026-07-04): `reward-opportunity-engine-design-v1.md` and `strategy-ranking-engine-design-v1.md` filed as **Reference** (module homes already decided — see `2026-07-04-mirror-notion-engine-design-docs.md`); `simulation-engine-spec-v1.md` filed as **Active**, the Phase 3 implementation reference. User/market research in `docs/research/`, UX journey in `docs/ux/`.
- Root `CLAUDE.md` build-ready: Backend Build Rules (8 hard rules), Skills & Agents triggers, phase = Phase 1, "no more planning documents". Root `README.md` current.
- Claude Code infra: skills (`tdd`, `tracker-sync`, `docs-sync`, `codebase-design`, `diagnosing-bugs`, `domain-modeling`, `grill-me`/`grilling`, `handoff`, `to-prd`, `to-issues`, `brandkit`, …) + subagents (`backend-reviewer`, `frontend-reviewer`, `feature-discussion`, `prd-writer`).

### In progress
- Nothing active.

### Next up
- Notion-side corrections to prevent re-drift (RED-001 "AI-assisted" marker, engine→module renames on ROE-001/SRE-001) — user or Claude-via-connector. Simulation stage is no longer missing (`simulation-engine-spec-v1.md` filed 2026-07-04).
- (Optional, non-blocking) Merge the two Strategy Ranking Engine drafts (2026-06-28 in `optimization-engine-spec-v1.md` + 2026-07-03 standalone) into one canonical section.
- (Optional, non-blocking) Consolidate `db-schema-v1.md` + build-plan §3 into a `db-schema-v2.md`.

---

## Last session notes

2026-07-15, 2nd sitting (guided-flow COMPLETE — slices 5–7, 9, 10) — Finished the revamp end to end. **Slice 5:** budget Continue silently probes `/simulations/probe`; hopeless goals get the inline interrupt (best-case vs required + AdjustmentMenu, steps stay editable); probe failures never block. **Slice 6:** the education step renders the wallet's reward story deterministically (shared-partner callouts, per-card rules/links); empty wallets skip it with the one-liner and get "Meet your recommended card(s)" education with the strategy (decision 13). **Slice 7:** opt-in split — Skip still yields a full strategy with the assumed-split caveat + one-click Refine; engaged split pre-fills the 25/20/20/15/10 template scaled to their total, categories win, payload switches to `spend_profile` monthly floors. **Slice 9:** wallet-can't-clear results show the WalletShortfallHero + wallet plan + explicit "Want strategies with additional cards?"; reveal shows the labeled pair with gold badges (the reviewer's one critical catch — stale tab selection surviving the reveal — fixed with remount keys and re-verified). **Slice 10:** homepage now embeds the wizard (one goal-entry everywhere), `goal-simulator.tsx` deleted (result rendering → `wizard/result.tsx`), and LLM education framing shipped as `GET /catalog/education/story` + `ai_reasoning/education.py` — number-echo validated (decimal-rate tokens can't be laundered), catalog-name hallucination guard, 10s timeout so a flaky OpenRouter call can't hang it (live-verified: null in ~10–14s), null = the deterministic step is the fallback; calculation framing continues to ride Stage-10 narration. 372 backend tests, mypy strict + ruff + tsc + eslint clean; three frontend-reviewer live passes + backend-reviewer passes, all findings applied and re-verified. **Nothing committed — the user handles commits.** Follow-ups now on Next up: HSBC-portal seed rows (canonical "16/24 via portal" education example), saved-goal signed-in live click-through, and the standing `/simulations` latency (~40s measured again; now the wizard's loader wait). _(1st sitting below.)_

2026-07-15 (guided-flow slices 4 + 8) — Continued straight from the 2026-07-14 sitting. **Slice 8 (backend, TDD):** the acquisition pair — `ranking-weights-v2.yaml` adds the versioned `acquisition_weights` profile (fees→cost / ecosystem→efficiency / transfer-quality→risk, rationale in the file; base weights identical to v1; app default flipped to v2), `select_acquisition_pair` + `select_guided_routes` in `optimization/ranking.py` reuse Stage 9's composite with the second profile — cheapest is picked by lowest card fee, NOT archetype (dedupe can erase the cheapest_viable label; discovered when the end-to-end golden failed), goal-missing candidates are never offered, same-set picks collapse to one combined-label route. backend-reviewer confirmed the math and raised one important point: flipping the default config changes route composition for the LIVE one-shot flow's wallet-can't-clear case — intentional (it IS the slice) and now pinned by an API-level test through the real `get_weights()` default, plus a golden for the collapse branch with genuinely distinct plans. Live: empty wallet → Magnus (best_value) + DCB (cheapest). **Slice 4 (frontend):** `goal-wizard.tsx` — the conversational-scroll shell (goal → cards → total → strategy) embedded at `/goals/new`; homepage keeps the one-shot until slices 6–7 exist (slice 10 does the swap + cleanup). frontend-reviewer drove six scenarios live on a temporary public preview route (deleted after) at both viewports: payloads correct (`total_spend_inr`, no `spend_profile`), edit-invalidation and blank-total honest, zero crit/important findings. 359 backend tests, mypy strict + ruff, tsc + eslint all clean. **Nothing committed — the user handles commits.** Next: slices 5 (early-stop via the probe), 6 (education step — needs the HSBC-portal seed rows first), 7 (opt-in split), then 9 and 10. Reviewer re-flagged the pre-existing `/simulations` latency (33–61s observed live, 30s budget) — now user-facing in the wizard's loader too; still open alongside the OpenRouter-key + DB-password rotations.

